node_modules/
.next/
out/
.env*.local
.DS_Store
coverage/

# Build artifacts the prebuild script (scripts/prebuild.mjs) regenerates on
# every build from topics/ — never hand-edited, never git-tracked (same
# rationale as out/ above; docs/bets/first-living-topic/technical-design/
# 04-data-design.md: "services/site/public/* ... is a pure, deterministic
# derivation of topics/ (rebuilt every time, never hand-edited, never
# git-tracked)").
public/rss.xml
public/skills/
