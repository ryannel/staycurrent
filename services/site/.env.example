# Environment Configuration
# Copy this file to .env.local and fill in the values
#
# Stay Current ships as a fully static export (see
# docs/architecture/decisions/0001-fully-static-site-no-servers.md) — there is
# no server-side runtime, so there are no server env vars to configure here.
