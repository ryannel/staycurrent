{
  "name": "staycurrent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:core": "cd core && pnpm install && pnpm build"
  },
  "devDependencies": {
    "@nx/devkit": "^23.0.1",
    "nx": "^23.0.1"
  }
}